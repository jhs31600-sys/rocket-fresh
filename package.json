{
  "name": "rocket-fresh-fill",
  "version": "1.0.0",
  "private": true,
  "description": "로켓프레시 장바구니 최소금액을 활용도 높은 식재료로 채우는 공개 배포용 웹앱",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "dev": "npx vercel@latest dev",
    "deploy": "npx vercel@latest --prod",
    "test": "node --test tests/*.test.cjs",
    "check": "node --check server.js && node --check api/health.js && node --check api/products.js && node --check api/deeplink.js && node --check lib/coupang.js && node --check public/app.js && node --check public/sw.js && node --check extension/content.js",
    "start": "node server.js"
  },
  "engines": {
    "node": "22.x"
  }
}
